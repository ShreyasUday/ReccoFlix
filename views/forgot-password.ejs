<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - ReccoFlix</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>

<body style="background-color: #1a1a1a; margin: 0; font-family: sans-serif;">
    <%- include('partials/header.ejs') %>

        <div class="auth-container">
            <div class="auth-card">
                <h2>Reset Password</h2>

                <% if (typeof error !=="undefined" ) { %>
                    <div class="auth-error">
                        <%= error %>
                    </div>
                    <% } %>

                        <% if (typeof success !=="undefined" ) { %>
                            <div class="auth-error"
                                style="background: rgba(46, 204, 113, 0.2); border-color: rgba(46, 204, 113, 0.5); color: #d4efdf;">
                                <%= success %>
                            </div>
                            <% } %>

                                <form action="/forgot-password" method="POST">
                                    <div class="form-group">
                                        <label for="email">Enter your registered email</label>
                                        <input type="email" id="email" name="email" required
                                            placeholder="example@email.com">
                                    </div>
                                    <button type="submit" class="btn-auth">Send Reset Link</button>
                                </form>

                                <a href="/login" class="auth-link">Back to Login</a>
            </div>
        </div>
</body>

</html>